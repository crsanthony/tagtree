'use strict';

var React = require('react/addons');

require('styles/GameView.sass');

var GameView = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for GameView</p>
        </div>
      );
  }
});

module.exports = GameView; 

